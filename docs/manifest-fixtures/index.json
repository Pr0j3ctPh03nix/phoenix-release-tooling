{
  "_note": "Conformance fixtures for manifest readers, generated by tools/gen_fixtures.py. Feed each `file` to the reader and assert `expect`. Do not edit by hand.",
  "schema": 3,
  "future_schema": 4,
  "payload_ids": [
    "game",
    "launcher",
    "mirrors",
    "mod"
  ],
  "signatures": "signatures/index.json",
  "expectations": {
    "accept": "The reader parses the manifest and proceeds normally.",
    "refuse:schema": "Rejected specifically because the schema is unsupported, with a message telling the user to update. NOT a parse error.",
    "refuse:invalid": "The schema is supported but the document cannot be safely acted on — a missing or unusable signing envelope (`payload_id`, `serial`), an unsafe `dest` (escapes the game root), or a violated bundle guarantee (the B1-B8 invariants in tools/validate_manifest.py). A broken release — NOT an 'update the app' message, and not a schema refusal.",
    "refuse:codec": "The schema is supported but a bundle names a codec the reader cannot decode. Same user-facing outcome as refuse:schema (tell the user to update); different detection point."
  },
  "cases": [
    {
      "file": "current.json",
      "expect": "accept",
      "why": "The current schema with every feature present: a raw entry, a multi-member bundle, a one-member bundle, a zero-byte entry and a BUNDLED OPTION VARIANT. A reader that rejects this is broken outright."
    },
    {
      "file": "schema2-options.json",
      "expect": "accept",
      "why": "The shape the mod producer emitted before its payload became a single bundle — schema 2, every entry named, no bundles. Every mod release published up to that cutover is this shape and must stay installable, so a schema-3 reader that only handles bundled documents would break on every release already in the wild."
    },
    {
      "file": "schema3-no-bundles.json",
      "expect": "accept",
      "why": "Schema 3 with no `bundles` key at all. Absent MUST mean none — a schema-3 document that bundles nothing is exactly a schema-2 document."
    },
    {
      "file": "legacy-no-schema.json",
      "expect": "accept",
      "why": "Schema 1 predates the `schema` key, so an ABSENT key means 1 — not 'unknown'. A reader that requires the key rejects every manifest ever published before it. It still carries the signing envelope: signatures are verified before a document is parsed, so the only documents that reach these checks come from a producer that signs — the absent-`schema` rule is orthogonal to that."
    },
    {
      "file": "future-schema.json",
      "expect": "refuse:schema",
      "why": "A manifest newer than the reader. Must fail with 'update the app', citing the schema — never a parse or validation error."
    },
    {
      "file": "future-unknown-option-kind.json",
      "expect": "refuse:schema",
      "why": "THE ORDERING TEST. Carries an option kind that does not exist, so a reader that parses the document before checking `schema` dies on the unknown kind and reports a parse failure. Correct behaviour is the SAME clean schema refusal as future-schema.json: read `schema` first, decide, only then parse."
    },
    {
      "file": "tree-dangling-ref.json",
      "expect": "accept",
      "why": "A `tree` node referencing a dest that files[] does not carry. The tree is PRESENTATIONAL, so this must not be fatal — skip the reference and install exactly as declared. A reader that refuses here turns a display slip into a client that cannot update at all."
    },
    {
      "file": "additive-unknown-keys.json",
      "expect": "accept",
      "why": "Same schema, unknown keys added at the top level, inside a file entry, inside a BUNDLE and inside an option. The producers treat additive keys as backward-compatible and do NOT bump `schema` for them, so a reader MUST ignore what it does not recognise. A reader that errors on unknown fields forces a needless bump for every addition."
    },
    {
      "file": "advisory-signed-at.json",
      "expect": "accept",
      "why": "`signed_at` years in the past, and still accepted. It is ADVISORY — display it, sort by it, never decide with it. A reader that ages a manifest out hands anyone with a wrong clock (or a release cut from an old branch) a client that can no longer update, and buys nothing: `serial` is the ordering authority and it is signed."
    },
    {
      "file": "missing-payload-id.json",
      "expect": "refuse:invalid",
      "why": "No `payload_id`. A signature proves who wrote a document, not what it is FOR, so the payload has to be stated inside the signed bytes and checked against what the client came for — otherwise a validly signed launcher manifest can be served as the mod manifest and installed as one. Missing means the check cannot be made."
    },
    {
      "file": "unknown-payload-id.json",
      "expect": "refuse:invalid",
      "why": "A `payload_id` outside the closed set. NOT 'update the app': the reader is not missing a feature, it has been handed a document it cannot dispatch, which means the release was mis-served or rewritten. Guessing from the contents is exactly the mistake signing exists to prevent."
    },
    {
      "file": "missing-serial.json",
      "expect": "refuse:invalid",
      "why": "No `serial`. Version strings are for humans and are not ordered (`1.10.0` vs `1.9.0`), so without a serial a mirror can replay a stale but perfectly signed manifest forever and no client can tell."
    },
    {
      "file": "serial-is-bool.json",
      "expect": "refuse:invalid",
      "why": "`serial` is JSON `true`. In several languages that IS an integer 1 — Python's isinstance(True, int) is true — so a reader that only checks 'is a whole number' silently orders releases by a boolean. The same trap `schema` already carries."
    },
    {
      "file": "serial-negative.json",
      "expect": "refuse:invalid",
      "why": "A negative `serial`. Nothing legitimately produces one, and a reader storing 'the highest serial installed' can be pushed below its own floor by a document claiming one."
    },
    {
      "file": "invalid-bundle-size-sum.json",
      "expect": "refuse:invalid",
      "why": "B2: the members' sizes no longer sum to the bundle's `size`. The reader splits a solid stream by counting bytes, so a wrong total silently misaligns every member after the error — it must be caught up front, not discovered as a hash mismatch."
    },
    {
      "file": "invalid-orphan-member.json",
      "expect": "refuse:invalid",
      "why": "B1: a member hash matching no entry in the document. Its size is therefore unknowable and the stream cannot be split at all."
    },
    {
      "file": "invalid-unbundled-entry.json",
      "expect": "refuse:invalid",
      "why": "B3: an entry with no `name`, a non-zero size, and its hash in no bundle. Nothing in the document says where its bytes come from."
    },
    {
      "file": "invalid-duplicate-member.json",
      "expect": "refuse:invalid",
      "why": "B5: one hash carried by two bundles. The reader would fetch and decode the same content twice, and 'the bundle holding X' stops being well defined."
    },
    {
      "file": "invalid-empty-bundle.json",
      "expect": "refuse:invalid",
      "why": "B7: a bundle with no members — an asset that costs a download and yields nothing."
    },
    {
      "file": "unknown-codec.json",
      "expect": "refuse:codec",
      "why": "A codec this build cannot decode, under a schema it CAN read. Adding a codec is supposed to bump `schema`, so this is a producer that broke that rule — but the user-facing answer is still 'update the app', never 'your download is corrupt'. Must be caught at parse time (R9), not deferred to when the bundle is needed."
    },
    {
      "file": "invalid-dest-traversal.json",
      "expect": "refuse:invalid",
      "why": "A `dest` with `..` components that escapes the game root. dest is the one field that turns a compromised manifest into an arbitrary file write, so a reader must REFUSE it rather than trust the producer — a supported schema, but not safely installable."
    }
  ]
}
